# ローカル開発用（Docker PostgreSQL）
DATABASE_URL=postgresql://dev:dev@localhost:5432/band_session

# セッション署名用シークレット（openssl rand -hex 32 で生成）
SESSION_SECRET=your-secret-here

# API トークン署名用シークレット（openssl rand -hex 32 で生成）
API_TOKEN_SECRET=your-secret-here
